# Local AI overlay files — may contain secrets, never commit
/CLAUDE.md
/CLAUDE_*.md
.claude/

# Local-only changelog (kept out of the published spec)
/rfc-changelog.md

# Go build/test artifacts
*.test
*.prof
*.out
/bin/
